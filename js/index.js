const botaoContrate = document.getElementById('btn_contrate');

botaoContrate.addEventListener('click', function (event) {
    event.preventDefault();

    alert("Site em manutenção, desculpe o transtorno!");
});

